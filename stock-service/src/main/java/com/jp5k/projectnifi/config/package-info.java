/**
 * Spring configuration classes for {@code stock-service}.
 *
 * <p>Classes here are {@code @Configuration} beans wiring up cross-cutting
 * concerns — security, messaging (RabbitMQ), and anything else that isn't
 * tied to a single feature and doesn't belong in the other packages.
 */
package com.jp5k.projectnifi.config;

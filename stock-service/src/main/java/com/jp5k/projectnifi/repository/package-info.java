/**
 * Spring Data JPA repositories for {@code stock-service}.
 *
 * <p>Interfaces here are the only place that talks to the database — thin
 * repository interfaces extending Spring Data JPA's repository abstractions,
 * with no business logic. Used exclusively by the {@code service} package.
 */
package com.jp5k.projectnifi.repository;

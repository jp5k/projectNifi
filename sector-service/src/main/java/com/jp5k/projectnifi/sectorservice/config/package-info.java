/**
 * Spring configuration classes for {@code sector-service}.
 *
 * <p>Classes here are {@code @Configuration} beans wiring up cross-cutting
 * concerns that aren't tied to a single feature and don't belong in the
 * other packages.
 */
package com.jp5k.projectnifi.sectorservice.config;

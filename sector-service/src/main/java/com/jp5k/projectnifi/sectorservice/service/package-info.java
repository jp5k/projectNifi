/**
 * Business/service layer for {@code sector-service}.
 *
 * <p>Classes here contain the application's business logic — orchestrating
 * calls to the {@code repository} package and enforcing rules that don't
 * belong on the entity itself. Controllers call into this package rather
 * than talking to repositories directly.
 */
package com.jp5k.projectnifi.sectorservice.service;

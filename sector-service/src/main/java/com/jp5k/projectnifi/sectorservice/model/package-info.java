/**
 * JPA entities for {@code sector-service}.
 *
 * <p>Classes here are persistent domain objects (e.g. {@code Sector}) mapped
 * directly to database tables via JPA annotations. Kept separate from the
 * {@code dto} package, which holds the shapes actually exposed over the API.
 */
package com.jp5k.projectnifi.sectorservice.model;
